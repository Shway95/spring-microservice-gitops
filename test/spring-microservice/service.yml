apiVersion: v1
kind: Service
metadata:
  name: spring-microservice
  namespace: test
  labels:
    app: spring-microservice
spec:
  type: ClusterIP
  selector:
    app: spring-microservice
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
